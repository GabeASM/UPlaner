import 'package:flutter/material.dart';

class CalificacioneScreen extends StatelessWidget {
  const CalificacioneScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Calificacione Screen'),
      ),
    );
  }
}
